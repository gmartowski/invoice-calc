import styled from 'styled-components';
import { holland, creme } from '../../variables.styled';

export const HeaderWrapper = styled.div`
    display: flex;
    justify-content: center;
    width:100%;
    background-color: ${holland};
    color: ${creme};
`;
